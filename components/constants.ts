import { Video } from '../types';

// This file is kept to prevent import errors, but the sample data has been removed.
// The application now loads all content directly from Firebase.
export const sampleVideos: Video[] = [];
